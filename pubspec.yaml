name: img_loader
description: "Fancy Image loader with image filtering."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  http:
  image:
  bloc:
  flutter_bloc:
  equatable:
  typed_data:
  path_provider:
  shimmer:
  google_fonts:
  shared_preferences:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true